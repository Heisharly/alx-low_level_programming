#include "main.c"
/**
 * _abs - function to find the absolute value of a number
 * @a: function parameters
 * Return: (-a or a)
 */
int _abs(int a)
{
	if (a < 0)
		return (-a);
	else
		return (a);
}

